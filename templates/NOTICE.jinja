{#-
NOTICE.jinja — minimal attribution that ships with the installed TARGET PROJECT
(H5/B2). Always generated (unconditional file, like .gitignore.jinja/
AGENTS.md.jinja) because `.harness/lib/docs_wiki_lint.py`, `ref_integrity.py`
and `scan_project.py` are ALWAYS installed, regardless of use_claude/use_codex —
and they carry third-party / same-author provenance that must travel with the
target project (CC BY-SA 4.0 Section 3(a)). The block for the 10 trailofbits
skills + diataxis + 3 internal skills is included only when
use_claude=true, because only then are they materialized under
.claude/skills/ (see templates/{% raw %}{% if use_claude %}{% endraw %}.claude{% raw %}{% endif %}{% endraw %}/skills/).
Full, always-current source: NOTICE + PROVENANCE.json at the root of the
orions-belt repo (github.com/<owner>/orions-belt) — this file is a SUMMARY
scoped to what this project specifically installed, not a full copy (see
orions-belt/NOTICE §8 and docs/manual/15-limitacoes-conhecidas.md item 14 for
why it is a summary and not a verbatim copy).
-#}
{{ project_name }} — NOTICE (third-party provenance inherited from the installed harness)
======================================================================

This project installed parts of code/skills licensed under **Creative Commons
Attribution-ShareAlike 4.0 International (CC BY-SA 4.0)** — full text at
https://creativecommons.org/licenses/by-sa/4.0/legalcode. This file is the
minimal attribution the license requires for the inherited material; the full
source (machine-readable, component-by-component inventory) is the
`NOTICE`/`PROVENANCE.json` of orions-belt itself (the framework that generated
this project), not of this repository.

## Always installed (`.harness/lib/`)

- `docs_wiki_lint.py`, `ref_integrity.py` — ported and parametrized from
  `guto-wiki` (same author as orions-belt; no third-party license conflict).
  Modified relative to the original.
- `scan_project.py` — a blueprint reimplementation (not a verbatim copy) of
  `scan-project.mjs` from Understand Anything
  (https://github.com/Egonex-AI/Understand-Anything, MIT License). MIT is
  permissive and does not require ShareAlike; credited here as good practice.

{% if use_claude %}
## Skills installed under `.claude/skills/` (use_claude=true)

- **Ten skills copied byte-for-byte from [trailofbits/skills](https://github.com/trailofbits/skills)**
  (commit `cfe5d7b1619e47fb5b38b7e2561dad7e5f1e89af`, CC BY-SA 4.0, made by
  Trail of Bits — https://www.trailofbits.com/): `semgrep`, `codeql`,
  `sarif-parsing`, `semgrep-rule-creator`, `variant-analysis`,
  `insecure-defaults`, `supply-chain-risk-auditor`, `agentic-actions-auditor`,
  `audit-context-building`, `fp-check`. Only the skill text/content is
  reproduced (no license conflict — same CC BY-SA 4.0 as this project). No
  endorsement by Trail of Bits of this project is implied (CC BY-SA 4.0 Section
  2(a)(6)). **The 10 upstream skills also shipped 10 Trail of Bits logos
  (`trail-of-bits-mark.svg`) and 10 per-skill `agents/openai.yaml` files. All 20
  files are excluded here: the logo is a trademark (separate from copyright,
  not granted by CC BY-SA), and the OpenAI metadata is outside this
  runtime-neutral content port.**
- **`diataxis`** — adapts the Diátaxis framework (https://diataxis.fr/, by
  Daniele Procida, CC BY-SA 4.0); it is a curated adaptation, not a verbatim
  copy of the site.
- **`owasp-security`, `curate-long-context`, `ai-ui-dev`** — extracted and
  generalized from a private production harness by the same author as
  orions-belt (original client name withheld by the author's decision); no
  third-party license conflict.

Full source (why each one, verification hash, license-compatibility review):
`NOTICE`/`PROVENANCE.json` of the orions-belt repository.
{% endif %}
