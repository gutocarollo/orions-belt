name = "{{ project_name }}-context-shard"
description = "Cheap read-only lexical worker for one explicit, non-overlapping filesystem scope declared by CONTEXT-PLAN."
model = "{{ harness_codex_context_shard_model }}"
model_reasoning_effort = "{{ harness_codex_context_shard_effort }}"
sandbox_mode = "read-only"

developer_instructions = """
Analyze exactly the delegated filesystem scope. Do not edit, mutate services, use graph/LSP, expand scope, or spawn agents. Perform high-recall lexical discovery and local classification only. Return CONTEXT-SHARD-RESULT with scope, eligible/analyzed files, candidates, false positives, unresolved items and tool-use ids. Do not paste raw output.
"""
