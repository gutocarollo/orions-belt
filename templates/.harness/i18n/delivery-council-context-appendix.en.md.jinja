
## Context Delivery precondition

A `WORKSPACE_WRITE` Council run created by `council_session.py` sets `context_required=true`. Before `PHASE-PLAN`, run `context-delivery`, persist `CONTEXT-PLAN`, execute its isolated workers, materialize hashed receipts and persist `CONTEXT-DELIVERY`. An informal Context Brief does not authorize planning.
