---
name: run-{{ project_name }}
description: Starts {{ project_name }} in local development using the project's configured canonical command. Use when the user asks to run/start the project or when live validation requires the dev stack.
---

# Run {{ project_name }} locally

This skill is generated only because an explicit project command was provided.
Do not substitute guessed topology, Compose, database or port commands for the
contract below.

## Start

From the repository root:

```bash
{{ harness_run_command }}
```

Configured development ports:

- web: `{{ harness_dev_web_port }}`
- API: `{{ harness_dev_api_port }}`
{%- if harness_dev_collab_port %}
- auxiliary/collaboration: `{{ harness_dev_collab_port }}`
{%- endif %}

{% if harness_run_health_command -%}
## Verify readiness

Run the read-only health contract after startup:

```bash
{{ harness_run_health_command }}
```

Do not report the stack as ready unless this command succeeds.
{%- else -%}
No automated health command is configured. Report process/port evidence and
state explicitly that application readiness was not proven.
{%- endif %}

{% if harness_run_stop_command -%}
## Stop

```bash
{{ harness_run_stop_command }}
```
{%- else -%}
Stop the foreground command with `Ctrl+C`. No additional shutdown command is
configured; do not invent a destructive cleanup command.
{%- endif %}

## Safety

- Run from the repository root.
- Never kill by global process name. Use only PIDs owned by this project.
- If the configured command is stale, stop and update the harness answers;
  do not silently replace the operational contract with an example.
