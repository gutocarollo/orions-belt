# Council run: council-macro-objective-upgrade

Objective: keep Council execution focused on the macro objective while proving
every critical path from request to release-ready delivery.

## Pendências não bloqueantes

- `D-LABEL-PARITY` — `DEFER_RUN`; phase/item labels may differ from descriptive
  labels in the execution graph, but edge ID, nodes, objective, test IDs and
  acceptance remain deterministically bound. Revisit if labels become
  machine-authoritative or user-facing routing keys.
