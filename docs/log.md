# Log — agent-harness

Cronológico append-only (mais recente primeiro). Formato: `## [YYYY-MM-DD] tipo · tópico — resumo`.
Convenção herdada do padrão Karpathy do repo-doador (learnhouse `docs/SCHEMA.md`) — ver
`planning/00-plano-consolidado.md` §1.

## [2026-07-20] feat · self-install (dogfood) + SessionStart freshness guard + report de decisão — (1) **INSTALL-REPORT.md** (`install_report.py`, `13a1598`): todo (re)install regenera `.harness/INSTALL-REPORT.md` com o que foi instalado/NÃO + flag que gateou + razão + exemplo aplicado + decisões file-level por estratégia + hook-manager; parte do processo, não extra. (2) **harness-freshness.sh** (`c4386fc`): hook SessionStart nos DOIS runtimes que no 1º prompt detecta instância ABSENT/STALE e injeta diretiva pro agente AVISAR o user; após decisão, `freshness-ack.sh` registra (para de avisar até o ref do template mudar). Warn-only default; `HARNESS_AUTOHEAL=1` opt-in reinstala sozinho se o installer for localizável. Regressão `test_harness_freshness.sh` (7 estados). (3) **Dogfood (D1 Opção B):** o orions-belt se auto-instalou no harness-wiki — AGENTS.md/CLAUDE.md idênticos, lessons-inject nos 2 runtimes, `core.hooksPath=.githooks` ativo (ref-integrity roda nos meus commits e passa). A instância renderizada DUPLICA `templates/`, então é gitignored (bloco em `.gitignore` acima do marcador orions-belt) e regenerável via `./harness-install.sh . --vcs-ref HEAD`; o canal de memória durável em git é ESTE `docs/log.md`. Um clone fresh sem instância é avisado pelo próprio freshness hook (quando a instância existe) ou, no harness-wiki, pelo `harness-install.sh` tracked na raiz. Suíte shell 17/17.

## [2026-07-20] test+fix · Claude audit round (v1.6.3) sobre a execução Codex (d7546aa) — auditoria adversarial REPRODUZÍVEL (3 subagents fable, ref congelado) reproduziu no MakersHub REAL o claim brownfield-safe: 0 bloqueante/0 alta. 2 MEDIA fechadas (8bd6708, tag v1.6.3): **G1** plano/stdout do install escreviam DENTRO do SCRATCH → `source_files()` os instalava (0-byte install-plan.stdout.json na raiz do target); fix = WORK dir separado + regressão. **#8** AGENTS.md default (use_ui_evidence=false) citava skill/gate `ui-evidence` inexistente → gateado no `{% if use_ui_evidence %}` (.en/.pt). **Teste brownfield no WhatsApp_Agent_Chat_slim-shape** (clone --local, 1647 tracked, exclui 119G untracked): fail-closed em 7 colisões de linhagem-harness, preservação byte-a-byte (só 3 merge surfaces), idempotência, edit-abort — TODAS as invariantes. **FINDING A (4b97f79):** `set_hooks_path.sh` só detectava Husky; repo com framework pre-commit (`.pre-commit-config.yaml`, core.hooksPath vazio) tinha os hooks silenciosamente bypassados por `core.hooksPath=.githooks` → fix = detectar + avisar (não setar), regressão `test_set_hooks_path_precommit.sh`. Suíte de eval reutilizável `eval/brownfield-install-eval.sh` + `eval/whatsapp-brownfield-2026-07-20/FINDINGS.md`. **Premissa de co-desenvolvimento Codex↔Claude auditada+TRAVADA (f802c7f, `test_codev_shared_memory.sh` 11/11):** CLAUDE.md==AGENTS.md byte-idêntico (fonte única via include), lessons-inject no SessionStart dos DOIS runtimes, `tasks/lessons.md`+`docs/log.md` arquivos únicos compartilhados, §16 capture→inject→promote, runs em `.harness/runs` neutro, ZERO dependência de memória nativa do Claude (Codex-cega). GAP declarado: o próprio harness-wiki NÃO se auto-instala (só `docs/log.md`; sem root AGENTS.md/lessons) — nosso co-dev flui por git+log+conversa, não pela shared-memory do próprio harness. Suíte shell 15/15.

## [2026-07-20] chore · follow-on interno englishizado — dev-tooling do próprio repo (não user-facing/não instalado) traduzido: `engine/lint/*` (docs_wiki_lint, ref_integrity), `engine/contract/scripts/*` (validate_contract + 5 siblings), `engine/_tooling_conf.py`, `engine/hooks/subagent-throttle.sh`, e os testes internos (`templates/tests/*.sh`, `templates/.harness/lib/tests/*.py`, engine tests) — comentários/docstrings/mensagens/labels. Contrato: preservados TODOS os argumentos de assertion (asseriam conteúdo `.pt` ou tokens fixos), regexes/matched-strings, fixtures (dados de teste), o registro factual do `engine/contract/README` (valor original de REQUIRED_SKILLS), e as commit-msgs internas dos test .sh. Suíte 100% verde após (engine+templates+lints). Sobra PT só onde é por-design: assertions que testam o modo pt (test_council_merge), fixtures, padrões de jargão do banlist, e `docs/planning` (arquivo de época). A documentação do repo (`docs/manual`, SCHEMA em PT) fica FORA deste follow-on — é a doc do produto, tradução separada se desejada.

## [2026-07-20] fix · EN mode is now actually English (i18n gap closure, Option A) — verificação multi-subagent (5 subagents) revelou que `harness_language=en` produzia inglês só na prosa das instruções; a CAMADA RUNTIME inteira (15 hooks + 10 lib/install scripts + settings.json + .gitignore + 4 understand/classname scripts) era português hardcoded, SEM camada de i18n. Option A (escolha do dono): runtime → **inglês-canônico** (como git/npm); prosa de instrução → bilíngue. Executado: (1) 8 skills faltantes → bilíngue (selector .en/.pt em .harness/i18n/); (2) ~11 prose-bugs nos .en existentes; (3) ~250 strings runtime englishizadas por subagents preservando lógica/tokens; (4) hooks de DETECÇÃO tornados bilíngues (completion-gate pega claim PT+EN; marathon-stop-gate pega Próxima ação/Next action + AGUARDANDO/WAITING) senão o gate não funcionaria no modo en; (5) 2 acoplamentos cross-file reconciliados (reap-leaks↔dev-doctor, marathon-gate↔skill); (6) periféricos (understand skill, test-auditor, 6 hookify rules, banlist) → inglês-canônico in place. Render EN: **zero PT user-facing** (excl. tests/engine internos + padrões de jargão do banlist). Modo PT preservado íntegro. Suíte 100% verde (council/scan/merge/contract/parity/brownfield/lints). FOLLOW-ON declarado: engine/ copies + tests/ internos seguem PT (dev tooling, não user-facing).

## [2026-07-20] feat · README v2 (product pitch, EN) + assets visuais — README reescrito de log-de-fases para pitch de produto em inglês no modelo Charm: hero SVG (constelação do Cinturão de Órion + wordmark, `assets/hero.svg`), terminal-cast SVG com output REAL do install (`assets/demo.svg`, fills inline camo-safe, 31 skills/15 hooks/189 arquivos verificados), badge row (shields.io estático), matriz de features com emoji, mermaid do pipeline plan→review→execute→verify, seções de dual-language/install/docs/license. Assets movidos p/ `assets/` (raiz, fora do escopo do docs-wiki-lint). SVGs renderizados via cairosvg e OLHADOS (regra pixels-não-diff). História de fases F0-F9 preservada em docs/log+planning. Lints verdes.

## [2026-07-20] feat · i18n: flag `harness_language` (en|pt) + tradução EN completa — instruções de prosa (AGENTS.md/CLAUDE.md, 8 skills-shared, loop) agora bilíngues via selector Jinja: `SKILL.md.jinja` virou `{% include "...SKILL." ~ harness_language ~ ".md.jinja" %}` selecionando `SKILL.{en,pt}.md.jinja`; idem AGENTS (`AGENTS.{en,pt}.md.jinja`) e loop (variantes em `.harness/i18n/`). Var copier `harness_language` **default `en`** (canônico p/ adoção; PT preservado como opção). Tradução PT→EN de ~2100 linhas por 6 subagents paralelos com contrato rígido: **tokens de contrato determinístico preservados verbatim** (PROVA-DE-CONCLUSAO, PLAN-ADVERSARIAL-VERIFICATION: SATISFEITO|REPLANEJAR|SABATINAR|BLOQUEADO, GATE-GRILL, severidades, field labels) — idênticos nos 2 idiomas, então gates/hooks independem do idioma. grill-me já era EN (fica single-file). `_exclude` dos variantes (só o selector renderiza). Testes adaptados p/ neutralidade de idioma: test_council_merge lê `.pt`; witness reaponta `.pt`; codex_parity extrai skills multi-frasing (skill `x`/`x` skill/$x); skill_runtime_parity grep sentinel neutro. Render en+pt verificado (AGENTS EN vs PT, sentinel presente nos 2, variantes não vazam, council byte-idêntico .claude/.agents). Suíte 100% verde.

## [2026-07-19] rename · harness-wiki → Orion's Belt (orions-belt) — repo renomeado no GitHub (`gutocarollo/orions-belt`, old redireciona) + rename interno global do identificador `harness-wiki`→`orions-belt` em 71 arquivos (140 ocorrências), EXCETO `docs/planning/**` (época, SCHEMA §4) e o dump `long-context.md`. Inclui o marcador funcional de merge `orions-belt:begin/end` (merge_docs.py + testes atualizados juntos — brownfield e2e passa). Nome de exibição "Orion's Belt" nos títulos H1 (README/llms.txt/manual); identificador `orions-belt` em contexto técnico. Prefixo de variáveis `harness_*` mantido (substantivo genérico — o produto É um harness). Dir local segue `harness-wiki` (renomear quebraria paths da sessão). Testes verdes: merge_docs, council_merge, scan, contract, codex_parity, skill_runtime_parity, brownfield e2e, docs_wiki_lint, ref_integrity.

## [2026-07-19] feat · check "frescor do grafo" no loop + dogfood do /loop no próprio orions-belt — `templates/.claude/loop.md.jinja` ganhou o check 7 (gated por `harness_understand_apps_root`): conta arquivos mudados sob o PROJECT_ROOT desde o `meta.json` gitCommitHash (com diff relativo obrigatório) e dispara `/understand` incremental quando passam de `HARNESS_GRAPH_REFRESH_THRESHOLD` (nova var copier, default 15, emitida no `harness.conf` dentro do bloco understand). Threshold porque `/understand` é LLM-heavy e roda no modelo da própria sessão do loop (assinatura, não API). Cap 08 atualizado (drift template×manual). **Dogfood:** `.claude/loop.md` materializado no próprio orions-belt (primeiro artefato tracked em `.claude/`), com os checks reais do framework (`engine/lint/*`, suíte de regressão, drift, árvore suja) — o framework passa a rodar sobre si o loop que instala. **1ª passada de loop executada ao vivo (evidência desta sessão):** docs-wiki-lint OK, contract OK, council/scan/parity OK; achou e corrigiu 2 findings reais — placeholders ilustrativos `slug.md`/`path.md` em `08-memory-surfaces.md` (→ allowlist) e o drift do cap 08 acima; reportou `.curate/`/`long-context.md` como órfãos untracked (fora do loop, decisão humana).

## [2026-07-19] feat · harness-init dual-runtime (fecha gap Codex) + fluxogramas de decisão por componente — `harness-init` migrou para o mecanismo `skills-shared` (fonte única `templates/.harness/skills-shared/harness-init/SKILL.md.jinja` + wrappers `.claude/skills/harness-init/` e `.agents/skills/harness-init/`, excluída nominalmente no `copier.yml`): fecha o gap Codex-only declarado no README (F5) — um projeto `use_codex=true`/`use_claude=false` agora tem invocação nativa do instalador guiado. Corpo byte-idêntico ao anterior (zero token Jinja; `AskUserQuestion` já era tratado como opcional). Verificado nos 3 renders (claude→só `.claude`, codex→só `.agents`, both→ambos byte-idênticos) + adicionado ao loop de byte-identidade do `test_codex_parity.sh`. Em paralelo, `docs/manual/14` ganhou a subseção "Árvore de decisão por componente condicional": fluxogramas mermaid por-componente (ui-evidence, ds-gate, deploy-prod-stack, understand-apps-incremental) + tabela dos demais, espelhando FIELMENTE as regras de `scan_project.py` (`_rule_*`) — resposta ao caso "instalar council num backend puro pula o ui-evidence" (ramo `NAO_APLICAVEL`). README: nota de pendência do gap Codex marcada como resolvida.

## [2026-07-18] feat · grill-me substitui clarification-plan + Gate Condicional de Grill no council — skill `grill-me` criada (fonte única `templates/.harness/skills-shared/grill-me/SKILL.md.jinja` + wrappers Claude/Codex, excluída nominalmente no `copier.yml` como as outras 6 skills dual-runtime), 100% genérica e 100% runtime-neutra, com ZERO conteúdo do Council dentro — a skill = a entrevista relentless + a disciplina de trade-offs D[n] (≥2 exemplos aplicados bons + 2 ruins reais + Opção C) que vivia na `clarification-plan`, agora fundidas. `clarification-plan` REMOVIDA (skills-shared + 2 wrappers). Integração no council SEM engessar o auto-loop (`AUTO_DECIDE=true` continua default): nova seção **Gate Condicional de Grill** em `delivery-council` (5 condições para `GATE-GRILL: SIM`; só interrompe em subjetividade material `ALTA`/`BLOQUEANTE` cuja resposta muda o plano) + status `SABATINAR` no Planning Adversarial Loop (`NECESSITA-GRILL`/`DECISOES-GRILL`, estado suspenso que não consome rodada). Contrato `SABATINAR` também em `adversarial-review` (nova Etapa 6.1) e nos 2 reviewer agents (`.md.jinja`/`.toml.jinja`). Refs atualizadas: `AGENTS.md.jinja` (3), `docs/manual/09` (tabela) e `docs/manual/11` (seção D[n] + loop + mermaid). Preservados como registro factual: `engine/contract/README` (documenta o hardcode ORIGINAL do doador) e `docs/planning/` (arquivo de época).

## [2026-07-18] fix · F9-fixes (engenharia) — rodada de correção do review adversarial final (`CORRIGIR`: 1 ALTA + 5 MEDIA + 3 BAIXA) na parte templates/engine/copier.yml, em paralelo à generalização de docs. O gap ALTA: `templates/AGENTS.md.jinja` renderizava afirmações FALSAS e marca de cliente no projeto-alvo (hash de grafo do doador como "atual", `--qq-green-*`/`data-theme queroquero/dracula`, bloco Dropdown/Charts de outro produto, `project_doc_max_bytes = 65536` contradizendo o render 32768, "máximo 6"/"Augusto" hardcoded) — reescrito: blocos de produto viraram PRINCÍPIOS genéricos, understand-anything gateado por `harness_understand_apps_root`, toda afirmação numérica usa a variável de config, proveniência virou comentário Jinja (não renderiza). Sweep repo-wide: grep de termos do doador = ZERO nos 4 cenários de render (inclusive IDs `hookify.qq-prod-*`→`prod-*` no classificador e até o verbo "quero" em prosa, que o gate casa). MEDIA: 7 variáveis-fachada LIGADAS (caps de rodada do council, strikes do marathon, model do adversarial-reviewer, 3 caps de reap/runaway), `dev-doctor.sh` GENÉRICO novo em `.harness/hooks/` (mata a referência morta `scripts/dev-doctor.sh` nos 2 runtimes; `reap` do Stop hook era no-op silencioso), marathon hooks leem `HARNESS_RUNS_DIR`, módulos `use_ui_evidence`/`use_icon_guard` (flags D5 no copier.yml, default true; off = zero artefato/registro). BAIXA: texto corrompido do harness-init Passo 6 reconstituído; help honesto de `project_root` (testado: `dst_path` é PurePosixPath sem `.absolute()`, sem filtro `abspath` — não normaliza no template); dedupe do regex prod-destroy via `unique`. Notas completas: `planning/notas-f9-fixes.md`.

## [2026-07-18] feat · F9-generalização (docs) — `docs/manual/` criado como a documentação canônica do PRODUTO: 15 capítulos genéricos (`01-visao-geral` … `13-understand-anything` reescritos do zero sobre o que o framework INSTALA — variáveis `HARNESS_*`/`PROD_*` citadas pelo nome, links para os arquivos reais de `templates/`/`engine/`, exemplos neutros; + `14-instalacao-e-update` novo, cobrindo `copier copy`/`harness-init`/`copier update`; + `15-limitacoes-conhecidas` consolidando os gaps antes espalhados) e índice em `docs/manual/README.md`. Gate duro cumprido: grep de termos do projeto-doador em `docs/manual/` = zero hits. Em commit subsequente da mesma rodada: `case-study/` (a cópia do tutorial específico do doador, F7) REMOVIDO do framework por ordem do dono — "sempre foi sobre ser um harness parametrizável", não sobre o doador; o repositório-fonte original do tutorial permanece existindo fora deste produto, e a história da construção permanece em `docs/planning/`. Índices da raiz (`README.md`/`llms.txt`/`SCHEMA.md`) reescritos: audiência "entender o harness" roteia para `docs/manual/`; proveniência vira 1 parágrafo sóbrio.

## [2026-07-18] fix · restructure templates/ — achado arquitetural do log anterior CONFIRMADO e corrigido com evidência da doc oficial do Copier (`docs/configuring.md`: inclusão condicional é via Jinja-no-NOME do path, `_subdirectory` não "achata" nada — não existe mecanismo de achatamento pós-cópia). `templates/claude/.claude/**` → `templates/{% if use_claude %}.claude{% endif %}/**`; `templates/codex/.codex/**` → `templates/{% if use_codex %}.codex{% endif %}/**`; `AGENTS.md.jinja` saiu de dentro do wrapper `.codex/` pra raiz de `templates/` como `{% if use_codex %}AGENTS.md{% endif %}.jinja` (`.jinja` FORA da condição); `templates/shared/**` → `templates/**` direto, sem wrapper. `use_claude`/`use_codex` agora realmente gateiam a árvore inteira (provado: `.claude/skills/...`/`.codex/agents/...` na RAIZ do projeto renderizado, sem wrapper `claude/`/`codex/`; `use_codex=false` → zero arquivo `.codex`). Materializado `.harness/lib/_tooling_conf.py` + `.claude/hooks/subagent-throttle.sh` (hooks rodam como comando shell local no projeto-alvo — não há como referenciar `engine/` de fora do repo; `settings.json.jinja` corrigido de volta pra `.claude/hooks/*`, os outros 16 hooks continuam não-portados). Materialização do Codex explicitamente ADIADA pra F4 (nenhum `.codex/hooks.json` existe ainda). `ref_integrity.py --staged` pegou 7 citações realmente obsoletas nesta mesma sessão (docs/READMEs citando os paths antigos) — todas corrigidas antes do commit.
## [2026-07-18] feat · F3 templatização (1-4 completos, 5 wiki-seeds completo, 3 skills parcial) — 12 hookify (`templates/{% if use_claude %}claude{% endif %}/`), 3 arquivos F1.5 renomeados p/ `{{ project_name }}-*`, 6 skills + 8 agents nomeados, `settings.json`/`config.toml` (17 hooks apontando `engine/hooks/*`), `docs/SCHEMA.md.jinja`+seeds de `log.md`/`lessons.md`. 2 bugs reais corrigidos: `use_claude`/`use_codex` nunca gateavam a geração das árvores (corrigido); regex de rename sobre-substituiu paths absolutos em 2 arquivos F1.5 (corrigido, `{% raw %}` protegendo comentários de proveniência). Achado arquitetural em aberto (não resolvido, flagueado): render aninha tudo sob `claude/`, `codex/`, `shared/` — hipótese é que F5 (`harness-init`) achata isso no destino final, não confirmado.
## [2026-07-17] feat · F1.6 des-drift AGENTS.md — `templates/codex/AGENTS.md.jinja` gerado como filtro determinístico de `.claude/CLAUDE.md` (remove o bloco `⭐ PRODUÇÃO`, mantém o resto, `{{ project_name }}`/`{{ project_root }}`/`{{ harness_dev_db_port }}`); achado real de drift adicional (§6 desatualizado, bullet §8 ausente) além do §15/§16 já sabido, documentado em `planning/notas-f1.6.md`.
## [2026-07-17] feat · F1.5 merge do council — fusão das 2 linhagens de `learnhouse-delivery-council/SKILL.md` (learnhouse × agent-swarm) em `templates/shared/.agents/skills/`, `templates/shared/.agents/skills/adversarial-review/` e `templates/codex/.codex/agents/learnhouse-adversarial-reviewer.toml`; REPLAN/FIX-REQUEST+CONSUMED (agent-swarm) fundido com subagent-obrigatório+REVISORES+SendMessage (learnhouse). Witness próprio (`templates/shared/verification/council-witness.json`, 6/6) via nova flag `--root` de `verify_witness.py`; 5/7 testes deferidos em F1 reativados em `templates/shared/tests/test_council_merge.py`.
## [2026-07-17] feat · F1 lint+contrato — porte de `docs_wiki_lint.py`/`ref_integrity.py` (guto-wiki, engine/lint/) e da pilha schemas/witness/ledger/validate (agent-swarm, engine/contract/), ambos parametrizados via `engine/_tooling_conf.py` em vez de duplicar o parser de config.
## [2026-07-17] feat · F0 config central — `engine/_tooling_conf.py`, `templates/.harness/harness.conf.jinja`, `templates/copier.yml`, hook `subagent-throttle.sh` parametrizado e provado com fixture.
## [2026-07-17] chore · bootstrap — skeleton do framework + plano consolidado (`planning/00-plano-consolidado.md`) e os 8 relatórios de pesquisa (`planning/research/`) que o fundamentam, copiados do scratchpad efêmero para dentro do repo.
